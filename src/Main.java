import javax.swing.*;

public class Main {
    public static void main(String[] args) {

        /*
        JFrame frame = new JFrame();
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // La X cierra el programa
        frame.setTitle("SPACE PROGRAM URSS"); // Título del frame
        frame.setResizable(false); // No se puede ajustar el tamaño
        frame.setSize(420, 420); // Tamaño del frame
        frame.setLayout(null); // Desactiva el LayoutManager predeterminado
        frame.setVisible(true); // Hacer visible el frame
        frame.setLocationRelativeTo(null); // Centrar el frame

        JLabel label = new JLabel();
        label.setText("Usuario: ");
        label.setBounds(150, 0, 300, 300); // Definir posición y tamaño de la etiqueta
        frame.add(label); // Añadir la etiqueta al frame
        */

        LaunchPage launchPage = new LaunchPage();
        //Mecanico mecanico = new Mecanico();

    }
}


